#include <stdio.h>
int main(void){
    long long int x,y;
    long long int a,b;
    
        scanf("%lld %lld",&x,&y);
        a=x+y;
        b=x-y;
        printf("%lld",a*b);
    
    return 0;
}
 
